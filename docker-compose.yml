version: '3.8'

services:
  momentumchat-manager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: momentumchat-manager-v2
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - momentumchat-network

  # Optional: Include MomentumChat API for complete stack
  # Uncomment the section below if you want to run MomentumChat API alongside the manager

  # momentumchat-api:
  #   image: momentumchat/api:latest
  #   ports:
  #     - "8080:8080"
  #   container_name: momentumchat-api
  #   restart: unless-stopped
  #   environment:
  #     - DATABASE_PROVIDER=postgresql
  #     - DATABASE_CONNECTION_URI=postgresql://postgres:password@postgres:5432/momentumchat
  #     - AUTHENTICATION_API_KEY=your-api-key-here
  #     - SERVER_PORT=8080
  #   depends_on:
  #     - postgres
  #     - redis
  #   networks:
  #     - momentumchat-network

  # postgres:
  #   image: postgres:15-alpine
  #   container_name: momentumchat-postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=momentumchat
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - momentumchat-network

  # redis:
  #   image: redis:7-alpine
  #   container_name: momentumchat-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - momentumchat-network

networks:
  momentumchat-network:
    driver: bridge

# volumes:
#   postgres_data:
